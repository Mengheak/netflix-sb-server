spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/netflix_heak_clone}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: mengheak088@gmail.com
    password: ${GOOGLE_APP_PASSWORD}
server:
  tomcat:
    max-swallow-size: -1
    max-http-form-post-size: -1
file:
  upload:
    video-dir: uploads/videos
    image-dir: uploads/images
app:
  cors:
    allowed-origins: ${CORS_ALLOW_ORIGINS}

